apiVersion: v1
kind: Service
metadata:
  name: lirbery
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lirbery"
    port: 8080
  selector:
    app: lirbery
