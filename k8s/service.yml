apiVersion: v1
kind: Service
metadata:
  name: partment
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "partment"
    port: 8080
  selector:
    app: partment
