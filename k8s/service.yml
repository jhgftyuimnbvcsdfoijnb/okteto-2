apiVersion: v1
kind: Service
metadata:
  name: wherkil
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wherkil"
    port: 8080
  selector:
    app: wherkil
