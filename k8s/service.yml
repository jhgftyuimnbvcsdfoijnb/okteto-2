apiVersion: v1
kind: Service
metadata:
  name: cultural
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cultural"
    port: 8080
  selector:
    app: cultural
